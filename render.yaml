services:
  - type: web
    name: autism-detect
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt && python -V
    startCommand: streamlit run app.py --server.port $PORT --server.headless true
    envVars:
      - key: DATABASE_URL
        value: sqlite:////tmp/asddb.sqlite3

